package com.example.movra.bc.personalization.behavior_profile.domain.type;

/**
 * 조정 제안이 가리키는 프로필 항목. insight의 DriftType과 name 기준으로 1:1 매핑된다.
 */
public enum ProfileAdjustmentTarget {
    FOCUS_HOURS,
    EXECUTION_DIFFICULTY,
    RECOVERY_STYLE
}
